spring:
  shell:
    interactive:
      enabled: false

  liquibase:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create

  sql:
    init:
      data-locations: data.sql
      mode: embedded
      continue-on-error: true